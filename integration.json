{
    "name": "Link Preview Generator",
    "type": "modifier",
    "description": "Extracts URLs from messages and generates previews with metadata.",
    "endpoints": {
        "preview": {
            "method": "POST",
            "url": "https://telexpreview.onrender.com/preview",
            "description": "Processes a message and returns link previews."
        }
    },
    "settings": {
        "enabled": true
    }
}
